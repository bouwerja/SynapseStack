import type { ConfigData } from 'html-validate';
import { HtmlValidate } from 'html-validate';
import type { LogLevel } from '../config.js';
export declare const getValidator: (options?: ConfigData) => HtmlValidate;
export declare const useChecker: (validator: HtmlValidate, usePrettier?: boolean, logLevel?: LogLevel) => {
    checkHTML: (url: string, html: string) => Promise<{
        valid: boolean;
        results: import("html-validate").Result[];
    }>;
    invalidPages: string[];
};
export declare function isIgnored(path: string, ignore?: Array<string | RegExp>): boolean;
